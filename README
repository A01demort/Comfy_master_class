1. Main_Docker파일에 있는 코드는 그대로 적용가능합니다.
ComfyUI & FLUX 마스터 클래스에서 사용되는 모든 커스텀 노드를 의존성에러까지 해결해서 넣은 코드입니다.


2. A1_parallel 코드는 이 강의에서 사용되는 허깅페이스 모델 약 16개를
병렬로 한번에 10개씩 다운받을 수 있는 코드입니다.
매우빠른속도로 처리가능합니다.

해줘야하는 부분은 허깅페이스 API_KEY만 바꿔주면 됩니다.


3. Restor코드
현재 코드의 허점은 모델은 Volume에 다운되어 runpod을 종료해도 살아있지만
커스텀노드를 ComfyUI-manager에서 설치한 다음 종료후 다시켰을때 Import Fail이 됩니다.

그 이유는, Docker파일이 리셋되면서 의존성 설치파일이 휘발되기 때문인데
이를 막기 위해 Restore코드를 돌리면 다시한번 어떤 커스텀노드에서 import fail이 떴는지 확인하고
설치를 한다음 Volume에 기록할 수 있게 따로 백업을 합니다.

이후, 재실행한다음 문제가 생기면 ex) restorcode.sh restore을 하면
바로 복구해서 사용할 수 있습니다.
